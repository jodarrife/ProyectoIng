import { FiltroActAsignadaPipe } from './filtro-act-asignada.pipe';

describe('FiltroActAsignadaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroActAsignadaPipe();
    expect(pipe).toBeTruthy();
  });
});
